name: domain
description: Entidades, contratos e casos de uso compartilhados do Kid's Task.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
