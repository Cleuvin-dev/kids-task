name: data_access
description: DTOs, clientes e repositórios de acesso a dados do Kid's Task.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  domain:
    path: ../domain
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1
  meta: ^1.16.0
  supabase_flutter: ^2.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
